'use strict';

const assert = require("power-assert");
const report = require("./../handler.js").report;

describe('handler.js', () => {

  // TODO configを環境変数で置き換えた後、テスト用のルーム等を指定できるようになったら、以下のテストを開放する。
  // it('report', () => {
  //   report({}, {}, (err, data) => { return; });
  // });
});
